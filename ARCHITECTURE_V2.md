# Architecture V2 — HelpDesk + Dynamic Admins + CRUD

## Новые требования и как я их закрыл как Senior

### 1. Диалог Админ ↔ Пользователь через Reply (Ключевое)

**Проблема прошлого подхода:** анкета улетала и терялась.

**Решение V2 — Ticket System на основе reply threading:**

```
Пользователь (бот)  --> анкета --> [Тикет #1: admin_msg_id=1024]
                                           |
Админ в админ-чате отвечает REPLY на 1024  --> бот перехватывает
       |
       +--> message.reply_to_message.message_id == 1024 ?
              Да -> ищем ticket по admin_message_id
                   -> берем user_id из тикета
                   -> bot.send_message(user_id, text, from admin)
                   |
Пользователь пишет еще что-то --> бот форвардит в админ-чат как reply к 1024
                                     (чтобы у админа был тред диалога)
```

**Технически:**
- Таблица `tickets (user_id, admin_message_id, admin_chat_id, status)`
- Handler `admin_reply_handler.py`: слушает ТОЛЬКО сообщения в ADMIN_CHAT_ID где `F.reply_to_message`
- Проверяет: `admin_message_id` есть в БД? Если да — это ответ клиенту.
- Поддержка медиа: text, photo, voice, document — через `copy_message` или `send_*`
- Ответ пользователю приходит **от имени бота** (важно для анонимности админа), но с пометкой `💬 Відповідь від адміністрації:`

### 2. Продолжение диалога
После закрытия анкеты FSM очищается. Любое следующее сообщение пользователя (не команда) попадает в `user_chat.py`:

- Есть тикет (пусть и закрытый) — форвард в админ-чат как `💬 Продолжение диалога` reply к сообщению заявки.
- Тикета нет, но в профиле выбрана услуга (бот перезапускался) — форвард без reply-якоря.
- Нет ни тикета, ни услуги — сообщение игнорируется: мусор в админ-чат не уходит.
- Связь «сообщение админ-чата → заявка» пишется в `admin_messages`, поэтому reply на reply
  любой вложенности находит заявку даже после рестарта.
- Поток сообщений ограничен `middlewares/throttling.py`.

### 3. Источник вступления (Канал)
Реализуем через deep-link: `t.me/your_bot?start=channel_xyz` или `?start=cpa_123`

В `start.py`:
```python
args = message.text.split() # /start channel_xyz
source = args[1] if len>1 else "direct"
```
Сохраняем в `users.source`. 

В приветственном сообщении:
```
🔗 Джерело: Канал @channel_xyz
```
И в админ-уведомлении тоже. Это закрывает требование "указывает источник отправки".

Если юзер реально вступил в канал где стоит бот — ловим `chat_member` event (бот должен быть админом канала).

### 4. Динамические права администратора
Требование: "всем пользователям, добавленным в админский чат — выдать права".

**Реализация:**
- Таблица `admins (user_id, added_at)`
- Handler `membership.py`:
  - `ContentType.NEW_CHAT_MEMBERS` в админ-чате -> add to DB
  - `ContentType.LEFT_CHAT_MEMBER` -> remove
  - `ChatMemberUpdated` filter для ADMIN_CHAT_ID -> отслеживаем `member` -> `administrator/member`
  - Bootstrapping: при старте бота делаем `get_chat_administrators(ADMIN_CHAT_ID)` и `get_chat_members` (если возможно) — сидируем таблицу.
  - Проверка прав: `crud.is_admin(user_id)` (таблица `admins`); в админ-чате права подразумеваются

Любой кто пишет в админ-чате автоматически становится админом (если его еще нет) — защита от рассинхрона.

### 5. Админ-панель — CRUD услуг + Статистика

Теперь услуги не в .py файле, а в БД `services` (SQLite). Это позволяет CRUD без редеплоя.

**Панель вызывается:**
- В админ-чате: `/admin`
- В личке админа: `/admin` (проверка прав)

**Меню `/admin`:**
```
🛡️ Адмін-панель

[ 📊 Статистика ] [ 🛠 Послуги ]
[ 👥 Адміни ]     [ 📢 Розсилка (опц) ]
```

**CRUD Послуг:**
- Список всех услуг (активные и выключенные), каждая -> карточка с кнопками
  «Редактировать», «Скопировать UA в RU», «Удалить», «Вкл/Выкл»
- Добавить: FSM `AddService` — 10 шагов (все тексты сразу на двух языках):
  1. ID (латиницей, `[a-z0-9_]{3,32}` — значение попадает в callback_data)
  2. Emoji
  3–4. Название UA, RU
  5–6. Краткое описание UA, RU
  7–8. Условия (HTML) UA, RU
  9–10. Текст кнопки UA, RU (уникальность по обоим языкам, лимит 64 символа)
- Редактировать: FSM `EditService` — язык → поле → новое значение
- Вкл/Выкл: `is_active`; выключенная услуга не открывается даже по старой клавиатуре

**Статистика:**
- `/stats` показывает: всего пользователей, сколько добавилось сегодня,
  открытые диалоги, оплаченные заявки, топ-услугу
- `/users` — последние 20 пользователей, по строке на каждого:
  ```
  ✅ 123456 | Ім'я з анкети | 25 | vip_docs | channel_top | @username
  ⛔ — забаненные (users.is_banned)
  ```

### 6. Обновленная структура проекта V2

```
app/
├── bot.py — точка входа: init_db, регистрация роутеров, polling, фоновые задачи
├── config.py — pydantic-settings (.env)
├── database/
│   ├── db.py — схема таблиц, миграции (WAL, двуязычные услуги, недостающие колонки)
│   └── crud.py — все запросы
├── data/
│   └── texts.py — UA/RU тексты клиента + RU тексты админа, t(key, lang)
├── handlers/
│   ├── start.py — /start, /lang, /cancel + source tracking
│   ├── services.py — выбор услуги из БД
│   ├── questionnaire.py — анкета 3 шага -> тикет
│   ├── user_chat.py — свободные сообщения юзера в тред админ-чата
│   └── admin/
│       ├── confirm_payment.py — Подтвердить/Отклонить, договорная цена, /confirm
│       ├── broadcast.py — регулярные рассылки (+ восстановление после рестарта)
│       ├── reply_handler.py — reply-мост админ -> юзер
│       ├── payment_check.py — проверка оплаты (клиент и админ)
│       ├── admin_panel.py — /admin меню, карточки услуг
│       ├── services_crud.py — FSM добавления/точечной правки услуг
│       ├── stats.py — /stats, /users
│       ├── membership.py — динамические админы + синхронизация при старте
│       └── debug_all.py — отладочный роутер (только DEBUG_ALL=1)
├── keyboards/
│   ├── reply.py — услуги из БД, кнопки анкеты
│   └── inline.py — язык, «Погоджуюсь», админ-панель, тикет, оплата
├── middlewares/
│   └── throttling.py — антифлуд свободных сообщений
├── services/
│   ├── cryptopay.py — Crypto Pay API (бессрочные счета, http/socks-прокси)
│   └── invoice_watcher.py — фоновая проверка оплаты пачками
├── states/
│   ├── questionnaire.py — Questionnaire
│   └── admin_states.py — AddService, EditService, ConfirmPayment, Broadcast
└── utils/
    ├── text.py — экранирование HTML, лимиты Telegram, форматирование сумм
    ├── telegram.py — edit/answer/reply для колбеков (в т.ч. InaccessibleMessage)
    └── callbacks.py — разбор callback_data
```

### 7. Безопасность
- Админ-ID не светится юзеру
- Все ответы идут от имени бота
- SQLite с WAL режимом
- Антифлуд: 5 свободных сообщений в 10 секунд на клиента, для админов — безлимит
- Чужой счёт нельзя подтвердить своей кнопкой (`checkpay:` проверяет владельца),
  оплата проводится атомарно и ровно один раз
- Пользовательский текст экранируется перед подстановкой в HTML-шаблоны,
  длины сообщений и подписей укладываются в лимиты Telegram (4096/1024/64)
- Прокси в логах маскируется (пароль не светится)
- Логи без персональных данных
