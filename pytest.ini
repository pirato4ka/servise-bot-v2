[pytest]
asyncio_mode = auto
# Фикстуры живут в пределах одного теста — иначе pytest-asyncio ругается
# на неопределённую область event loop и сулит смену поведения в будущих версиях.
asyncio_default_fixture_loop_scope = function
testpaths = tests
python_files = test_*.py
